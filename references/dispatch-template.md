# 调度文档模板

这是团队的共享作战地图。你（Agent）的所有工作上下文都在这里。
复制此模板，填充实际内容后作为Agent Teams的中心调度文档。

**Agent 强制指令**：CEO 在下发任务时，会在 prompt 中要求你先读本文档。如果你没有收到这条指令，主动找到并阅读 `DISPATCH.md`。

这是团队唯一的共享上下文。**不读 DISPATCH.md 就开始编码 = 在真空中协作**，一定会导致上下游数据格式不匹配、接缝断裂、返工。

---

# [项目名] - 团队调度文档

> **日期**: YYYY-MM-DD
> **项目根目录**: [如 ./洛克王国PVP/]
> **本轮目标**: [一句话描述本轮要解决的核心问题]

---

## 一、项目现状

### 已完成部分
- [列出当前已完成的模块/数据/基础]

### 存在问题
- [列出需要重构/修复的核心问题]

### 技术债
- [从上轮继承的、本轮不处理但需跟踪的问题]

### 数据源
- [唯一数据源路径，如 ./数据库/]
- [关键数据量：如 229精灵/469技能/1925BUFF]

---

## 二、不可变约束

### 设计/业务约束
1. [约束1：如 伤害公式系数不可改]
2. [约束2：如 数据源只从指定目录读取]
3. [约束3：如 业务规则：没有STAB/暴击/命中率]

### 代码约束
4. [约束4：如 代码只在指定目录内开发]
5. [约束5：如 每个Agent只修改自己边界内的文件]

### 测试纪律（自动生效，无需CEO逐任务指定）
6. **所有实现必须包含对应测试**。例外（纯CSS、纯配置变更）需在进度汇报中说明理由
7. **测试类型自选**：单元测试验证逻辑，集成测试验证接缝，端到端验证链路。Agent根据任务性质自行判断需要哪种。判断不了的在进度汇报中说明
8. **禁止假绿**：测试必须包含边界用例和错误场景。只测"输入A→输出A"而不测空值/异常/极限的测试视为未完成

---

## 三、协作规则

### 依赖与等待
- 当你的任务依赖另一个Agent提供的接口，且该Agent尚未完成时，**必须等待**，不要自行猜测接口格式或自行实现替代方案
- 等待期间可以先完成不依赖该接口的部分
- 如果你发现等待时间过长（超过你预期的合理时间），在进度汇报中说明阻塞原因
- **禁止自建数据通道**：如果你的模块需要数据，必须通过架构约定的数据源（如DataLoader）获取。如果约定数据源还没提供你需要的接口，等待而不是自己open()读文件

### 进度汇报
- 完成开发后，在本文档的"进度追踪"章节更新你的状态：⬜未启动 → 🔄进行中 → ✅已完成
- 完成时简要说明：实现了什么、对外接口是否和契约一致、有没有偏离规格的地方
- 如果发现规格有问题或无法满足，立即在汇报中说明，不要自行绕过

### 禁止回退策略（强制）
- **不得采取任何降低预期效果的回退策略。** 遇到困难时，不是绕过它——是停下来。
- 如果你发现某个依赖不可用、某个数据找不到、某个工具报错，**诚实汇报**，附上你试过的方法和失败原因，**停下手中任务**，等待CEO反馈。
- 不要自己做降级处理（如"API用不了那我换网页搜""数据缺失那我编一个默认值""视频没字幕那我猜内容"）。你的工作是报告真实情况，决策由CEO做。

### 冲突处理
- 只修改自己边界内的文件，绝不触碰其他Agent的文件
- 如果发现需要修改别人的文件才能完成自己的任务，不要动手——在汇报中说明原因，由CEO协调
- 如果两个Agent对同一接口的理解不一致，以本文档的接口契约为准，不要私下协商修改

### 数据对接
- 严格按照本文档的接口契约和接缝定义开发，不要自行发明数据格式
- 如果契约中的示例数据和你实际拿到的数据不一致，在汇报中说明，不要自行适配

---

## 四、依赖图与调度顺序

```
[画出Agent间的依赖关系]

示例：
  A（数据层，无依赖）
  ├── B（依赖A的接口）
  └── C（依赖A的接口）
  D（无依赖，可与A并行）

调度顺序：
  第一波：A + D（无依赖，并行启动）
  → CEO验收A →
  第二波：B + C（依赖A，并行启动）
```

---

## 五、团队定义

### Agent A: [模块名]
**代号**: [短名]
**文件边界**: [可修改的文件列表]
**禁止触碰**: [绝对不能碰的文件列表]
**依赖**: [依赖哪些Agent的产出，或"无"]

#### 任务背景
[为什么需要这个模块，当前存在什么问题]

#### 最终规格
1. [规格1：只要求结果]
2. [规格2：不规定过程]

#### 对外接口
```python
# 示例调用和返回值（具体到字段名、值类型）
result = agent_a.do_something("输入")
# → {'field1': 123, 'field2': 'value'}  # int, str
```

---

### Agent B: [模块名]
[同上结构]

---

## 六、接口契约与接缝定义

### 接口清单

#### Agent A 对外接口
```python
# 附带完整示例数据
from xxx import DataLoader
loader = DataLoader()
data = loader.get_xxx("示例名")
# data 结构: {'key1': value1_type, 'key2': value2_type}
# 示例: {'name': '迪莫', 'hp': 400, 'element': '光'}
```

#### Agent B 对外接口
[同上]

### 接缝定义

| 接缝位置 | 上游输出 | 下游期望 | 转换责任方 |
|---------|---------|---------|-----------|
| A→B | [格式+示例数据] | [格式+示例数据] | [Agent代号] |
| B→C | [格式+示例数据] | [格式+示例数据] | [Agent代号] |

---

## 七、进度追踪

| Agent | 状态 | 完成说明 |
|-------|------|---------|
| A | ⬜未启动 | - |
| B | ⬜未启动 | - |

---

## 历史记录

### [日期] 第N轮：[本轮目标摘要]
- **结果**：[完成/部分完成/未完成]
- **产出**：[关键产出物]
- **遗留技术债**：[本轮发现但未处理的问题]
- **经验教训**：[值得记录的教训]
